@page
@model Keswa.Pages.Inventory.IndexModel
@{
    ViewData["Title"] = "إدارة المخازن";
}

<!-- Page Header -->
<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h1 class="h2 fw-bold">@ViewData["Title"]</h1>
        <p class="text-muted">عرض وإدارة جميع المخازن المتاحة في النظام.</p>
    </div>
    <div>
        <a asp-page="Create" class="btn btn-primary"><i class="bi bi-plus-circle me-2"></i>إضافة مخزن جديد</a>
    </div>
</div>

<!-- Search Bar -->
<div class="card shadow-sm border-0 mb-4">
    <div class="card-body">
        <form method="get">
            <div class="row g-3 align-items-center">
                <div class="col">
                    <input asp-for="SearchTerm" class="form-control" placeholder="ابحث باسم المخزن..." />
                </div>
                <div class="col-auto">
                    <button type="submit" class="btn btn-secondary"><i class="bi bi-search me-2"></i>بحث</button>
                </div>
                <div class="col-auto">
                    <a asp-page="./Index" class="btn btn-outline-secondary">عرض الكل</a>
                </div>
            </div>
        </form>
    </div>
</div>

<!-- Warehouses Table -->
<div class="card shadow-sm border-0">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-hover align-middle">
                <thead class="table-light">
                    <tr>
                        <th>اسم المخزن</th>
                        <th>الموقع</th>
                        <!-- *** تم إضافة هذا العمود *** -->
                        <th>نوع المخزن</th>
                        <th>عدد الأصناف</th>
                        <th>إجراءات</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.Warehouses)
                    {
                        <tr>
                            <td><strong>@Html.DisplayFor(modelItem => item.Name)</strong></td>
                            <td>@Html.DisplayFor(modelItem => item.Location)</td>
                            <!-- *** تم إضافة هذا الحقل *** -->
                            <td>@Html.DisplayFor(modelItem => item.Type)</td>
                            <td>@item.InventoryItems.Count()</td>
                            <td>
                                <a asp-page="/Inventory/Details" asp-route-id="@item.Id" class="btn btn-sm btn-outline-secondary" title="عرض الأصناف"><i class="bi bi-eye"></i></a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        <!-- Pagination Controls -->
        @if (Model.TotalPages > 1)
        {
            <nav aria-label="Page navigation">
                <ul class="pagination justify-content-center mt-4">
                    <li class="page-item @(Model.HasPreviousPage ? "" : "disabled")">
                        <a class="page-link" asp-page="./Index"
                           asp-route-SearchTerm="@Model.SearchTerm"
                           asp-route-CurrentPage="@(Model.CurrentPage - 1)">السابق</a>
                    </li>
                    @for (var i = 1; i <= Model.TotalPages; i++)
                    {
                        <li class="page-item @(i == Model.CurrentPage ? "active" : "")">
                            <a class="page-link" asp-page="./Index"
                               asp-route-SearchTerm="@Model.SearchTerm"
                               asp-route-CurrentPage="@i">@i</a>
                        </li>
                    }
                    <li class="page-item @(Model.HasNextPage ? "" : "disabled")">
                        <a class="page-link" asp-page="./Index"
                           asp-route-SearchTerm="@Model.SearchTerm"
                           asp-route-CurrentPage="@(Model.CurrentPage + 1)">التالي</a>
                    </li>
                </ul>
            </nav>
        }
    </div>
</div>
