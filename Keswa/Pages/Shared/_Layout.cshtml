<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - نظام كسوة ERP</title>

    <!-- Google Fonts: Tajawal -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Tajawal:wght@400;500;700&display=swap" rel="stylesheet">

    <!-- Bootstrap & Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.rtl.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.css" />

    <!-- Select2 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
    <link href="https://cdn.jsdelivr.net/npm/select2-bootstrap-5-theme@1.3.0/dist/select2-bootstrap-5-theme.min.css" rel="stylesheet" />

    <style>
        body {
            font-family: 'Tajawal', sans-serif;
            background-color: #f8f9fa;
        }

        .app-container {
            display: flex;
            min-height: 100vh;
            overflow-x: hidden;
        }

        /* --- Sidebar --- */
        .app-sidebar {
            width: 250px;
            /* التحسين: تطبيق خلفية متدرجة رأسية بألوان هادئة وأكثر وضوحًا */
            background: linear-gradient(180deg, #ffffff 0%, #f0f2f5 100%);
            border-left: 1px solid #e9ecef; /* لون أفتح للحدود */
            position: fixed;
            top: 0;
            bottom: 0;
            right: 0;
            z-index: 1050;
            overflow-y: auto;
            box-shadow: -2px 0 12px rgba(0,0,0,0.08); /* ظل أخف */
            transition: width 0.3s ease, transform 0.3s ease;
        }

        .sidebar-header {
            padding: 1rem;
            text-align: center;
            border-bottom: 1px solid #e9ecef;
            transition: padding 0.3s ease;
        }

        .sidebar-logo {
            max-width: 120px;
            height: auto;
            border-radius: 8px;
            transition: max-width 0.3s ease, opacity 0.3s ease;
        }

        .sidebar-nav {
            padding: 0;
            margin: 0;
            list-style: none;
        }

            .sidebar-nav .nav-item {
                margin: 0.25rem 0;
            }

            .sidebar-nav .nav-link {
                display: flex;
                align-items: center;
                gap: 12px;
                padding: 0.75rem 1rem;
                color: #333;
                transition: background 0.2s;
                text-decoration: none;
                white-space: nowrap;
                overflow: hidden;
            }

                .sidebar-nav .nav-link:hover {
                    background: #e9ecef;
                }

            .sidebar-nav .menu-trigger {
                font-weight: bold;
                color: #555;
            }

                .sidebar-nav .menu-trigger .arrow-icon {
                    transition: transform 0.3s ease, opacity 0.3s ease;
                }

                .sidebar-nav .menu-trigger:not(.collapsed) .arrow-icon {
                    transform: rotate(-180deg);
                }

        .nested-menu {
            padding-right: 1.5rem;
            background-color: #f8f9fa;
        }

            .nested-menu .nav-link {
                padding: 0.5rem 1rem;
                font-size: 0.9rem;
            }

                .nested-menu .nav-link.active {
                    font-weight: bold;
                    color: var(--bs-primary);
                }

        /* Collapsed Sidebar Styles for Desktop */
        .app-sidebar.collapsed {
            width: 80px;
        }

            .app-sidebar.collapsed .sidebar-header {
                padding: 0.75rem;
            }

            .app-sidebar.collapsed .sidebar-logo {
                max-width: 45px;
            }

            .app-sidebar.collapsed .sidebar-nav .nav-link span,
            .app-sidebar.collapsed .sidebar-nav .arrow-icon {
                opacity: 0;
                width: 0;
                pointer-events: none;
            }

            .app-sidebar.collapsed .sidebar-nav .nav-link {
                justify-content: center;
                gap: 0;
            }

                .app-sidebar.collapsed .sidebar-nav .nav-link:hover::after {
                    content: attr(data-bs-title);
                    position: absolute;
                    right: 90px;
                    background: #343a40;
                    color: white;
                    padding: 0.5rem 1rem;
                    border-radius: 5px;
                    font-size: 0.85rem;
                    z-index: 1100;
                }

            .app-sidebar.collapsed .nested-menu {
                display: none !important;
            }

            .app-sidebar.collapsed .menu-trigger.collapsed + .collapse.show,
            .app-sidebar.collapsed .menu-trigger:not(.collapsed) + .collapse.show {
                display: none !important;
            }

        /* Main Content */
        .app-main {
            flex: 1;
            margin-right: 250px;
            transition: margin-right 0.3s ease;
        }

            .app-main.full-width {
                margin-right: 80px;
            }

        .app-header {
            background: #fff;
            padding: 0.5rem 1rem;
            border-bottom: 1px solid #ddd;
            display: flex;
            align-items: center;
        }

        .app-content {
            flex: 1;
            padding: 1.5rem;
        }

        /* Overlay & Responsive */
        .sidebar-overlay {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0,0,0,0.3);
            z-index: 1040;
            display: none;
        }

            .sidebar-overlay.active {
                display: block;
            }

        @@media (max-width: 992px) {
            .app-sidebar {
                transform: translateX(250px);
            }

                .app-sidebar.open {
                    transform: translateX(0);
                }

                .app-sidebar.collapsed {
                    width: 250px;
                    transform: translateX(250px);
                }

            .app-main, .app-main.full-width {
                margin-right: 0;
            }
        }
    </style>
</head>
<body>
    <div class="app-container">
        <!-- Sidebar -->
        <aside class="app-sidebar" id="sidebar">
            <div class="sidebar-header">
                <img src="~/images/logo.png" alt="Logo" class="sidebar-logo">
            </div>

            <ul class="sidebar-nav">
                <li class="nav-item">
                    <a asp-page="/Index" class="nav-link" data-bs-title="لوحة التحكم"><i class="bi bi-speedometer2"></i><span>لوحة التحكم</span></a>
                </li>

                <!-- Inventory Management Section -->
                <li class="nav-item">
                    <a class="nav-link menu-trigger collapsed" data-bs-toggle="collapse" href="#inventoryMenu" role="button" data-bs-title="إدارة المخزون">
                        <i class="bi bi-archive-fill"></i>
                        <span>إدارة المخزون</span>
                        <i class="bi bi-chevron-down ms-auto arrow-icon"></i>
                    </a>
                    <div class="collapse" id="inventoryMenu">
                        <ul class="list-unstyled nested-menu">
                            <li><a asp-page="/Inventory/Index" class="nav-link"><span>المخازن</span></a></li>
                            <li><a asp-page="/Materials/Index" class="nav-link"><span>المواد الخام</span></a></li>
                            <li><a asp-page="/Colors/Index" class="nav-link"><span>إدارة الألوان</span></a></li>
                            <li><a asp-page="/GoodsReceipts/Index" class="nav-link"><span>سندات الاستلام</span></a></li>
                            <li><a asp-page="/MaterialRequisitions/Index" class="nav-link"><span>طلبات الصرف</span></a></li>
                        </ul>
                    </div>
                </li>

                <!-- Production Management -->
                <li class="nav-item">
                    <a class="nav-link menu-trigger collapsed" data-bs-toggle="collapse" href="#productionMenu" role="button" data-bs-title="إدارة الإنتاج">
                        <i class="bi bi-cpu-fill"></i>
                        <span>إدارة الإنتاج</span>
                        <i class="bi bi-chevron-down ms-auto arrow-icon"></i>
                    </a>
                    <div class="collapse" id="productionMenu">
                        <ul class="list-unstyled nested-menu">
                            <li><a asp-page="/Products/Index" class="nav-link"><span>إدارة الموديلات</span></a></li>
                            <li><a asp-page="/WorkOrders/Index" class="nav-link"><span>أوامر الشغل</span></a></li>
                        </ul>
                    </div>
                </li>

                <!-- Production Tracking Section -->
                <li class="nav-item">
                    <a class="nav-link menu-trigger collapsed" data-bs-toggle="collapse" href="#trackingMenu" role="button" data-bs-title="متابعة الإنتاج">
                        <i class="bi bi-binoculars-fill"></i>
                        <span>متابعة الإنتاج</span>
                        <i class="bi bi-chevron-down ms-auto arrow-icon"></i>
                    </a>
                    <div class="collapse" id="trackingMenu">
                        <ul class="list-unstyled nested-menu">
                            <li><a asp-page="/Departments/Cutting" class="nav-link"><span>قسم القص</span></a></li>
                            <li><a asp-page="/Departments/CuttingCompleted" class="nav-link"><span>القص المكتمل</span></a></li>
                        </ul>
                    </div>
                </li>

                <!-- Human Resources Section -->
                <li class="nav-item">
                    <a class="nav-link menu-trigger collapsed" data-bs-toggle="collapse" href="#hrMenu" role="button" data-bs-title="الموارد البشرية">
                        <i class="bi bi-people-fill"></i>
                        <span>الموارد البشرية</span>
                        <i class="bi bi-chevron-down ms-auto arrow-icon"></i>
                    </a>
                    <div class="collapse" id="hrMenu">
                        <ul class="list-unstyled nested-menu">
                            <li><a asp-page="/Workers/Index" class="nav-link"><span>إدارة العمال</span></a></li>
                        </ul>
                    </div>
                </li>

                <!-- Sales Management Section -->
                <li class="nav-item">
                    <a class="nav-link menu-trigger collapsed" data-bs-toggle="collapse" href="#salesMenu" role="button" data-bs-title="إدارة المبيعات">
                        <i class="bi bi-cart4"></i>
                        <span>إدارة المبيعات</span>
                        <i class="bi bi-chevron-down ms-auto arrow-icon"></i>
                    </a>
                    <div class="collapse" id="salesMenu">
                        <ul class="list-unstyled nested-menu">
                            <li><a asp-page="/Customers/Index" class="nav-link"><span>إدارة العملاء</span></a></li>
                            <li><a asp-page="/SalesOrders/Index" class="nav-link"><span>إدارة الطلبيات</span></a></li>
                        </ul>
                    </div>
                </li>

                <!-- Administration Section -->
                <li class="nav-item">
                    <a class="nav-link menu-trigger collapsed" data-bs-toggle="collapse" href="#adminMenu" role="button" data-bs-title="الإدارة">
                        <i class="bi bi-shield-lock-fill"></i>
                        <span>الإدارة</span>
                        <i class="bi bi-chevron-down ms-auto arrow-icon"></i>
                    </a>
                    <div class="collapse" id="adminMenu">
                        <ul class="list-unstyled nested-menu">
                            <li><a asp-page="/Administration/AuditLog" class="nav-link"><span>سجل المراجعة</span></a></li>
                        </ul>
                    </div>

                    <div class="collapse" id="adminMenu">
                        <ul class="list-unstyled nested-menu">
                            <li>
                                <a class="nav-link text-dark" asp-area="" asp-page="/Administration/DeleteAllData">
                                    <span>   حذف كل البيانات</span>
                                </a>
                            </li>
                        </ul>
                    </div>

                </li>



            </ul>
        </aside>
        <div class="sidebar-overlay" id="sidebar-overlay"></div>

        <!-- Main -->
        <div class="app-main" id="main">
            <header class="app-header">
                <button class="btn btn-icon btn-light" id="sidebar-toggle-btn">
                    <i class="bi bi-list"></i>
                </button>
                <div class="d-flex align-items-center ms-auto">
                    <partial name="_LoginPartial" />
                </div>
            </header>
            <main class="app-content">
                @RenderBody()
            </main>
        </div>
    </div>

    <!-- Scripts -->
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>

    <script>
        $(document).ready(function () {
            const sidebar = $("#sidebar");
            const main = $("#main");
            const overlay = $("#sidebar-overlay");
            const isDesktop = () => window.innerWidth >= 992;

            function applySidebarState() {
                if (isDesktop()) {
                    sidebar.removeClass("open");
                    overlay.removeClass("active");
                    if (localStorage.getItem('sidebarState') === 'collapsed') {
                        sidebar.addClass('collapsed');
                        main.addClass('full-width');
                    } else {
                        sidebar.removeClass('collapsed');
                        main.removeClass('full-width');
                    }
                } else {
                    sidebar.removeClass("collapsed").addClass("closed");
                    main.removeClass("full-width");
                }
            }

            $("#sidebar-toggle-btn").on("click", function () {
                if (isDesktop()) {
                    sidebar.toggleClass("collapsed");
                    main.toggleClass("full-width");
                    localStorage.setItem('sidebarState', sidebar.hasClass('collapsed') ? 'collapsed' : 'expanded');
                    if (sidebar.hasClass('collapsed')) {
                        $('.sidebar-nav .collapse.show').removeClass('show');
                        $('.sidebar-nav .menu-trigger').addClass('collapsed');
                    }
                } else {
                    sidebar.toggleClass("open closed");
                    overlay.toggleClass("active");
                }
            });

            overlay.on("click", () => {
                sidebar.removeClass("open").addClass("closed");
                overlay.removeClass("active");
            });

            $(".sidebar-nav .nav-link:not(.menu-trigger)").on("click", function () {
                if (!isDesktop() && sidebar.hasClass("open")) {
                    sidebar.removeClass("open").addClass("closed");
                    overlay.removeClass("active");
                }
            });

            // Auto-open current menu section
            var activeLink = $('.sidebar-nav .nested-menu .nav-link.active');
            if (activeLink.length > 0) {
                var parentCollapse = activeLink.closest('.collapse');
                if (parentCollapse.length > 0 && !sidebar.hasClass('collapsed')) {
                    new bootstrap.Collapse(parentCollapse[0], { toggle: true });
                }
            }

            applySidebarState();
            $(window).resize(applySidebarState);
        });
    </script>

    @if (!string.IsNullOrEmpty(TempData["SuccessMessage"] as string))
    {
        <script>toastr.success('@Html.Raw(TempData["SuccessMessage"])');</script>
    }
    @if (!string.IsNullOrEmpty(TempData["ErrorMessage"] as string))
    {
        <script>toastr.error('@Html.Raw(TempData["ErrorMessage"])');</script>
    }

    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>

